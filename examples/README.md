# Examples

## Flowgraphs

| File | Description |
|------|-------------|
| [`flowgraphs/usrp_sweep_demo.grc`](flowgraphs/usrp_sweep_demo.grc) | Minimal real-time panorama: Sweep → Time Sink + helpers |

Open with:

```bash
./run_gnuradio_companion.sh examples/flowgraphs/usrp_sweep_demo.grc
```

Adjust **Device Args** for your radio (`type=b200` or `addr=...`) before Run.
