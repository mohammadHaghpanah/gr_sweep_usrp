# Tools

Optional utilities for local debugging. **Not required** to build or use the OOT.

| Script | Purpose |
|--------|---------|
| `repro_headless.py` | Headless smoke check of the `usrp_sweep` source (no GUI) |

Prefer `../run_gnuradio_companion.sh` for normal use.
